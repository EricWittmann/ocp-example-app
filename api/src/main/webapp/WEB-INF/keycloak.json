{
	"realm": "${env.OCP_KC_REALM:ocp-example-app}",
	"bearer-only": true,
	"auth-server-url": "${env.OCP_KC_AUTH_URL:http://bluejay.paalin.org:8180/auth}",
	"ssl-required": "NONE",
	"enable-cors": true,
	"resource": "${env.OCP_KC_CLIENT_ID_API:api}"
}
